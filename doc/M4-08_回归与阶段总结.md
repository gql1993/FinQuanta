# M4-08 回归与阶段总结

日期：2026-04-10  
范围：`M4-01` ~ `M4-08`

## 1. 目标与结论

- 目标：对 `M4`（注册中心、配置开关、统一事件、结构化日志、指标追踪）进行阶段回归，并形成可追溯结论。
- 结论：`M4` 阶段目标已达到“第一版可运行、可验证、可观测”标准，可进入后续维护与增强阶段。

## 2. 本次回归执行记录

### 2.1 回归命令

```bash
python -m pytest tests/refactor/test_core_refactor.py
python infra/smoke_refactor_core.py
python -m uvicorn api_server.main:app --host 127.0.0.1 --port 9000
python infra/smoke_api.py
```

### 2.2 回归结果摘要

- `pytest`：`19 passed`
- `smoke_refactor_core`：全部检查项 `PASS`
- `smoke_api`：全部检查项 `PASS`

关键观测点（抽样）：

- 运行模式：`local/platform` 判定正常
- registry：`provider/strategy/notifier/workflow` 计数与 meta 正常
- 事件与日志：结构化日志与 `trace_id/decision_id` 链路可用
- 指标与追踪：`metrics/tracing` 骨架能力可用
- ops center：增量同步（`registry_token`）行为正常

## 3. M4 能力落地清单

- `M4-01` ~ `M4-04`：registry + feature flags/settings center 已落地
- `M4-05`：统一事件模型已落地（`core/audit/event_models.py`）
- `M4-06`：结构化日志已落地（`core/observability/structured_logging.py`）
- `M4-07`：指标与追踪骨架已落地（`core/observability/metrics.py`、`core/observability/tracing.py`）
- `M4-08`：回归与阶段总结已完成（本文档）

## 4. 遗留问题清单（下一阶段建议）

1. 观测能力目前为轻量骨架，尚未接入外部监控后端（如 Prometheus / OTEL Collector）。
2. 缺少桌面端与 Web 端自动化端到端回归（当前以 API/核心冒烟为主）。
3. 缺少长期趋势报表（如审批耗时分布、关键事件计数趋势）与告警策略。

## 5. 建议后续动作

- 先补一轮 `M5` 风格的“可运维增强包”：指标导出、trace 采样策略、关键告警阈值。
- 增加最小 E2E 回归脚本（至少覆盖登录、ops center、审批链路）。
- 将阶段总结纳入固定模板，每次里程碑完成后滚动更新。

## 6. 后续增强阶段进展（滚动）

- 增强-01（已完成）：新增 `GET /api/observability/metrics` 与 `GET /api/observability/alerts`，支持阈值化告警评估。
- 增强-02（已完成）：新增 `infra/e2e_minimal.py`，覆盖登录、ops center、审批链路（拒绝用例，无交易副作用）。
- 增强-03（已完成）：新增 Prometheus/OTEL 风格指标导出（`/api/observability/metrics/prometheus`、`/api/observability/metrics/otel`）。
- 增强-04（已完成）：新增审批/事件趋势报表与告警策略固化（`/api/observability/trends`、`/api/observability/alerts/policy`）。
- 增强-05（已完成）：新增 trace 采样策略与上下文传播骨架（`traceparent` 解析/注入、`/api/observability/traces*`）。
- 增强-06（已完成）：新增策略级告警路由骨架（通知/抑制/升级，`/api/observability/alerts/routing*`）。
- 增强-07（已完成）：新增 Collector 真连接与批量上报骨架（重试/超时/熔断，`/api/observability/collector/*`）。
- 增强-08（已完成）：新增告警路由执行器（真实通知发送 + 回执记录，`/api/observability/alerts/dispatch*`）。
- 增强-09（已完成）：补齐跨服务 trace 关联（task/workflow/openclaw 透传 `traceparent`，并在结果中回传 trace 关联信息）。
- 增强-10（已完成）：新增 trace 可检索能力（`/api/observability/traces/index` 与 `/api/observability/traces/trace/{trace_id}`）。
- 增强-11（已完成）：新增跨进程 trace 可视化对接能力（`/api/observability/traces/otel?trace_id=...` + Collector `trace_id` 定向上报）。
- 增强-12（已完成）：新增 Tempo/Jaeger 后端预设与 dashboard 字段模板（`/api/observability/traces/backends/presets`、`/api/observability/dashboard/template`）。
- 增强-13（已完成）：新增可落地面板聚合输入与示例（`/api/observability/dashboard/panel-input` + `infra/dashboard_examples/*` + `infra/demo_trace_panel_input.py`）。
- 增强-14（已完成）：新增 Grafana provisioning 自动化（`infra/setup_grafana_provisioning.py` + `infra/grafana/provisioning/*`）。
- 增强-15（已完成）：新增 Docker Compose 观测栈一键启动（Grafana + Tempo + 预置 provisioning，`infra/observability/docker-compose.observability.yml` + `infra/oneclick_observability_stack.py`）。
- 增强-16（已完成）：新增 Loki + Promtail 日志联动与三联面板（trace/alerts/logs，`grafana_trace_alerts_logs_trilink_dashboard.json`）。
- 增强-17（已完成）：补安全与运维加固（观测只读 token、日志轮转、oneclick 预检查、`check_observability_stack.py` 健康检查）。
- 增强-18（已完成）：补端到端观测演示与运维手册（录屏脚本 + 运维流程 + 常见故障排查，`doc/观测栈端到端演示与运维手册.md`）。
